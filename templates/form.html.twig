<form method="post">
    {% include "fields/text-input.html.twig" with {
        name: 'fullname',
        label: 'Vardas, pavardė',
        required: true,
        value: form.name.value,
        valid: form.name.valid
    } %}

    {% include "fields/text-input.html.twig" with {
        name: 'birthdate',
        label: 'Gimimo data',
        required: true,
        value: form.birthday.value,
        valid: form.birthday.valid
    } %}

    {% include "fields/text-input.html.twig" with {
        name: 'email',
        label: 'El.pašto adresas',
        required: false,
        value: form.email.value,
        valid: form.email.valid
    } %}

    {% include "fields/textarea-input.html.twig" with {
        name: 'message',
        label: 'Jūsų žinutė',
        required: true,
        value: form.content.value,
        valid: form.content.valid
    } %}
    <p>
        <span>* - privalomi laukai</span>
        <input type="submit" value="Skelbti" />
        <img src="img/ajax-loader.gif" alt="" />
    </p>
</form>
